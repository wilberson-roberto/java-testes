// Criar uma interface chamada calculos com três métodos: double somar(x,y), void subtracao(x,y) e double raiz(x), criar uma classe calculadora1 que implemente os métodos, será necessário criar uma classe principal para executar os testes.

public interface Calculos {
    double somar(double x, double y);
    void subtracao(double x, double y);
    double raiz(double x);
}
